services:
  ms-catalog:
    image: ms-catalog:latest
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8081:8080"
    environment:
      - SPRING_PROFILES_ACTIVE=dev
    networks:
      - ms-network

networks:
  ms-network:
    driver: bridge